export const schemaVersion = 1;

export const placeholderSchemaNotes = [
  "Phase 1 keeps SQLite as a project foundation stub.",
  "Full business tables arrive in the local data foundation phase.",
] as const;
